{% extends "layout.njk" %}

{% block content %}
    <form class="card">
        <section 
            class="image"
            style="background: url({{ teacher.avatar_url }}) no-repeat center center / cover;"
        ></section>
        <section class="details">
            <h3>Detalhes</h3>

            <div class="item">
                <div>Nome completo</div>
                <div>
                    {{ teacher.name }}
                </div>
            </div>

            <div class="item">
                <div>Data de nascimento</div>
                <div>
                    {{ teacher.age }}
                </div>
            </div>

            <div class="item">
                <div>Grau de escolaridade</div>
                <div>
                    
                </div>
            </div>

            <div class="item">
                <div>Tipo de aula</div>
                <div>
                    {{ 'Presencial' if teacher.type_class == 'presential' else 'À distância' }}
                </div>
            </div>

            <div class="item">
                <div>Área de atuação</div>
                <div>
                    {% for service in teacher.services %}
                        <span>{{ service }}</span>
                    {% endfor %}
                </div>
            </div>

             <div class="item">
                <div>Desde</div>
                <div>
                    {{ teacher.created_at }}
                </div>
            </div>

            <a href='#' class="button">Editar</a>

        </section>
    </form>
{% endblock %}